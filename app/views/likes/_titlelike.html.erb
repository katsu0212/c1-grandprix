<% if user_signed_in? && current_user.id != title.user.id %>
  <% if current_user.title_by?(title.id)  %>
    <td>
      <%= link_to destroy_like_path(title), method: :DELETE, remote: true do %> 
        <i class="far fa-grin-squint-tears unlike-btn"></i> 
      <% end %> 
      <%= title.likes.count %>
    </td>
  <% else %>
    <td>
      <%= link_to create_like_path(title), method: :POST, remote: true do %> 
        <i class="far fa-grin-squint-tears like-btn"></i> 
      <% end %> 
      <%= title.likes.count %>
    </td>
  <% end %>
<% end %>

